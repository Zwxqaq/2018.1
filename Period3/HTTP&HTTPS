1特点
  客户服务器模式，快速，灵活，无连接（一次请求后即断开），无状态（协议对于事务处理没有记忆能力）
2请求结构
  请求方法  URL  协议版本（请求头） 请求行
  头部字段  值                     请求行
  头部字段  值                     请求行
  请求正文
3响应结构
  协议版本 状态码  状态码描述（响应头） 状态行
  头部字段 值
  头部字段 值
  响应正文
4步骤
  客户端连接服务器
  发送http请求
  服务端接收请求返回http响应
  释放连接tcp连接
    close方式 请求后即断开
    keepalive方式 请求后保持一段时间再断开
  客户端浏览器解析html内容
5流程
  dns解析  域名解析成ip:port
  tcp连接
  发送http请求
  服务器处理请求并返回http报文
  浏览器解析渲染页面
  连接释放
6http状态码
  1xx  请求已接收，请继续处理
  2xx  成功接收，理解，接受
  3xx  重定向，请求需进一步操作
  4xx  客户端错误，语法错误或请求无法实现
  5xx  服务器端错误，服务器未能实现合法请求
  200  正常
  400  客户端请求有语法错误
  401  请求未授权，必须和www-Authenticate报头一起使用
  403  服务器接收请求，但是拒绝提供服务
  404  请求资源不存在，错误url等
  500  服务器不可预期错误
  503  服务器当前不能处理客户端请求，一段时间后可恢复正常
7get/post请求区别
  get url  post 报文体中
  get有长度限制，而且明文信息
  get可以被缓存，被存储，post不行
8cookie和session区别
  cookie文本文件形式保存在客户端
  客户端再次请求的时候，会把cookie回发  放在请求头
  服务端接收后，会解析cookie生成与客户端响应内容
  session保存在服务器上
  解析客户端请求并操作session id，按需保存信息
  setCookie的时候保存 jsessionid
  
  session相对cookie更安全
  如果减轻服务器负担，应当使用cookie
  cookie也可以保存信息的啊，之间艾融面试的时候说的 = =
9https和http区别
  http： http  tcp  ip
  https：http  ssl/tls  tcp  ip
  ssl安全版的http
    security sockets layer
    安全协议
    ssl3.0以后更名为tls
    采用身份验证和数据加密
  加密方式：
    对称加密
      加密和解密都用同一个密钥
    非对称加密：
      加密和解密使用的密钥是不同的
    哈希算法
      md5...等
    数字签名
      证明某个消息或者文件是某人发出的
  https需要CA申请证书，http不需要
  https密文传输，http明文传输
  连接方式不同，https端口443，http端口80
  https=http+加密+认证+完整性保护，较http安全
10https传输流程
  浏览器将支持的加密算法发送给服务器
  服务器选择一套浏览器支持的加密算法，以证书形式回发浏览器
  浏览器验证证书合法性，并结合证书公钥加密信息发送服务器
  服务器使用私钥解密，验证哈希，加密响应信息回发浏览器
  浏览器解密响应信息，并对消息验证，之后进行加密交互数据
